<%= form_for(@ruby2scaffold) do |f| %>
  <% if @ruby2scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ruby2scaffold.errors.count, "error") %> prohibited this ruby2scaffold from being saved:</h2>

      <ul>
      <% @ruby2scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
